const { createApp } = require("./app"); const { getConfig, createLogger } = require("@wallet/common"); const config = getConfig("payment-integration-service"); const { app } = createApp({ config, logger: createLogger("payment-integration-service") }); app.listen(config.port, () => createLogger("payment-integration-service").info({ port: config.port }, "payment-integration-service listening"));
