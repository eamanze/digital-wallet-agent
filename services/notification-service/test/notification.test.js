const test = require("node:test"); const assert = require("node:assert/strict"); const { render, mask, safeAmount } = require("../src/templates"); const { channelsFor } = require("../src/app");
test("templates mask identifiers and format minor-unit amounts", () => { const value = render("transaction.successful", { transaction_reference: "TX-123456789", amount_minor: 125050, currency: "NGN" }); assert.match(value.body, /TX-••••789/); assert.match(value.body, /1250\.50/); assert.equal(mask("abcdefghi"), "abc••••ghi"); assert.equal(safeAmount("secret"), "an amount"); });
test("security events use enabled security channels", () => { const channels = channelsFor("fraud.case_created", { email_enabled: true, sms_enabled: false, push_enabled: true, security_enabled: true, transaction_enabled: false }); assert.deepEqual(channels, ["email", "push"]); });
test("transaction events obey transaction preference", () => { const channels = channelsFor("transaction.successful", { email_enabled: true, sms_enabled: true, push_enabled: true, security_enabled: true, transaction_enabled: false }); assert.deepEqual(channels, []); });
