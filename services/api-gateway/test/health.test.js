const test=require("node:test");const assert=require("node:assert/strict");const {createApp}=require("../src/app");const {inject}=require("../../../tests/helpers/inject-express");
test("gateway live health returns ok",async()=>{const {app}=createApp({config:{serviceName:"api-gateway",jwtAccessSecret:"x",jwtRefreshSecret:"x"},logger:{warn(){}}});const response=await inject(app,{method:"GET",path:"/health/live"});assert.equal(response.status,200);});
